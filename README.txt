Temporary location for implementation of stubbed out ui. The javascript is actually fetching data from server, but the server is just responding with hard coded fake data. This is just to illustrate what the interactions with the front end would feel like.

To run, simply point your document root to this location.
